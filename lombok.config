# this is root dir: don't search for parent
config.stopBubbling = true

# add @Generated to Lombok code (so Jacoco will ignore Lombok generated code in reports)
lombok.addLombokGeneratedAnnotation = true
